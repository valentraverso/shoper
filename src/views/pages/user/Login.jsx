export default function Login(){
    return(
        <div>
            <form>
                <label htmlFor="email">Email</label>
                <input id='email' name='email' type="text" value='hola' />
                <button>Login</button>
            </form>
        </div>
    )
}